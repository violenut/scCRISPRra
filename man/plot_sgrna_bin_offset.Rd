% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{plot_sgrna_bin_offset}
\alias{plot_sgrna_bin_offset}
\title{per-sgrna 各 bin 的频率偏移条形图}
\usage{
plot_sgrna_bin_offset(
  global_hist,
  phenotype,
  sgrna_calls,
  target_sgrnas,
  normalize_by_sqrt = FALSE
)
}
\arguments{
\item{global_hist}{data.frame（来自 \code{\link{compute_global_hist}}）}

\item{phenotype}{命名数值向量}

\item{sgrna_calls}{boolean matrix（sgRNA × cell）}

\item{target_sgrnas}{字符向量}

\item{normalize_by_sqrt}{是否除以 \code{sqrt(global_freq)}（与原脚本 L352 一致），
默认 FALSE}
}
\value{
ggplot 对象（facet by sgrna）
}
\description{
对每个目标 sgRNA，画每个 bin 上
\code{(sgRNA 频率 - 全局频率)} 的条形图，可看出该 sgRNA
相对全局分布的偏离方向。
}
\examples{
\dontrun{
brks <- compute_breaks(phenotype)
gh <- compute_global_hist(phenotype, brks)
plot_sgrna_bin_offset(gh, phenotype, calls,
                      target_sgrnas = c("g1_1"))
}
}
