% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shape_decomp.R
\name{decompose_shape}
\alias{decompose_shape}
\title{对单个 sgRNA 的表型分布做正交多项式分解}
\usage{
decompose_shape(per_sgrna_phenotype, global_hist, degree = 2L)
}
\arguments{
\item{per_sgrna_phenotype}{命名数值向量，该 sgRNA 携带 cell 的表型}

\item{global_hist}{data.frame（来自 \code{\link{compute_global_hist}}）}

\item{degree}{多项式阶数，默认 2}
}
\value{
命名数值向量，长度 = degree，名称为
  \code{linear, quadratic, cubic, deg4, ...}
}
\description{
把该 sgRNA 携带的 cell 的表型分布与全局分布相减得到偏移，
再用 \code{stats::poly()} 的正交多项式基投影得到系数。
}
\examples{
pheno <- setNames(rnorm(100), paste0("cell", 1:100))
brks <- compute_breaks(pheno)
global_hist <- compute_global_hist(pheno, brks)
sub_pheno <- pheno[1:30]
shape <- decompose_shape(sub_pheno, global_hist, degree = 2)
}
