% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rra.R
\name{alpha_rra_v2}
\alias{alpha_rra_v2}
\title{MAGeCK 风格的 RRA 算法（v2，带 n_eff 收缩）}
\usage{
alpha_rra_v2(df, N_total = NULL, alpha = 0.05, fdr_method = "BH")
}
\arguments{
\item{df}{data.frame，必须含列：\code{gene, sgrna, rank}}

\item{N_total}{总 sgRNA 数（用于 ratio_rank 归一化），默认 \code{max(df$rank)}}

\item{alpha}{阈值参数，默认 0.05}

\item{fdr_method}{p 值校正方法，默认 \code{"BH"}}
}
\value{
同 \code{\link{alpha_rra}}，附加列 \code{n_eff}
}
\description{
与 \code{\link{alpha_rra}} 的区别：用 \code{n_eff = max(j_high, min(2, n))}
替代原始 n 作为 Beta 分布的 shape2 参数，对 sgRNA 数较少的 gene
做 shrinkage，缓解小样本下 p 值过乐观的问题。
}
\examples{
df <- data.frame(
  gene = c("g1", "g1", "g2", "g2"),
  sgrna = c("s1", "s2", "s3", "s4"),
  rank = c(1, 2, 50, 100)
)
alpha_rra_v2(df, alpha = 0.1)
}
