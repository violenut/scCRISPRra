% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{plot_rra_volcano_both}
\alias{plot_rra_volcano_both}
\title{同时画 high/low 两侧的火山图}
\usage{
plot_rra_volcano_both(
  rra_result,
  sig_threshold = 0.01,
  ylim_max = 1.2,
  midpoint = 10
)
}
\arguments{
\item{rra_result}{data.frame（来自 \code{\link{run_rra_pipeline}} 的某个元素）}

\item{sig_threshold}{显著性阈值（与 \code{run_rra_pipeline} 的 \code{sig_threshold} 一致），
默认 0.01；用于决定 alpha 透明度与 label 显示}

\item{ylim_max}{y 轴上限，默认 1.2}

\item{midpoint}{\code{scale_color_gradient2} 的 midpoint，默认 10}
}
\value{
gtable 对象（grid.arrange 输出）
}
\description{
调用 \code{\link{plot_rra_volcano}} 两次（high 和 low），
用 \code{gridExtra::grid.arrange} 拼成左右两图。
}
\examples{
\dontrun{
plot_rra_volcano_both(rra_out[["linear"]])
}
}
