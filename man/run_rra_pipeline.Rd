% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rra.R
\name{run_rra_pipeline}
\alias{run_rra_pipeline}
\title{对 shape 系数表跑 RRA 流程（join library 在此发生）}
\usage{
run_rra_pipeline(
  sgrna_summary,
  score_cols = c("linear", "quadratic"),
  library_df,
  rra_alpha = 0.1,
  rra_variant = c("v1", "v2"),
  fdr_method = "BH",
  sig_threshold = 0.01
)
}
\arguments{
\item{sgrna_summary}{data.frame（来自 \code{\link{summarize_sgrna_shapes}}），
必须含列 \code{sgrna}（sgRNA id）}

\item{score_cols}{字符向量，要跑 RRA 的 shape 系数列名，
默认 \code{c("linear", "quadratic")}}

\item{library_df}{data.frame（来自 \code{\link{load_sgrna_library}}），
含 \code{sgrna_id, sequence, gene_name}}

\item{rra_alpha}{RRA alpha 阈值，默认 0.1}

\item{rra_variant}{\code{"v1"} 或 \code{"v2"}}

\item{fdr_method}{p 值校正方法，默认 \code{"BH"}}

\item{sig_threshold}{显著性阈值，用于 alpha/genename 标注，默认 0.01}
}
\value{
list，长度 = length(score_cols)，每个元素是带 score/rank/
  p_two/alpha/genename 的 data.frame
}
\description{
这是 stage 3 的主函数：把 shape 分解输出的 sgRNA-level 系数表
与 library join，得到 gene 分组，然后对每个 score 列单独跑
\code{\link{alpha_rra}} 或 \code{\link{alpha_rra_v2}}。
}
\details{
输出包含 score/rank_high/rank_low/p_two/alpha_high/alpha_low/
genename_high/genename_low 等可视化辅助列，可直接喂给
\code{\link{plot_rra_volcano}}。
}
\examples{
\dontrun{
sgrna_summary <- summarize_sgrna_shapes(pheno, calls)
lib <- load_sgrna_library("library.txt", sep = "\t")
rra_out <- run_rra_pipeline(sgrna_summary,
                             score_cols = c("linear", "quadratic"),
                             library_df = lib)
}
}
