% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{plot_rra_volcano}
\alias{plot_rra_volcano}
\title{RRA 火山图（high 或 low 侧）}
\usage{
plot_rra_volcano(
  rra_result,
  direction = c("high", "low"),
  sig_threshold = 0.01,
  ylim_max = 1.2,
  midpoint = 10
)
}
\arguments{
\item{rra_result}{data.frame（来自 \code{\link{run_rra_pipeline}} 的某个元素）}

\item{direction}{\code{"high"} 或 \code{"low"}}

\item{sig_threshold}{显著性阈值（与 \code{run_rra_pipeline} 的 \code{sig_threshold} 一致），
默认 0.01；用于决定 alpha 透明度与 label 显示}

\item{ylim_max}{y 轴上限，默认 1.2}

\item{midpoint}{\code{scale_color_gradient2} 的 midpoint，默认 10}
}
\value{
ggplot 对象
}
\description{
\code{direction = "high"} 画 p_high 侧（检验 sgRNA 聚集在 shape 系数低端），
\code{direction = "low"} 画 p_low 侧（聚集在高端）。
x 轴为 score（gene 的 shape 系数均值），y 轴为 -log10(FDR)，
显著 gene 的 label 自动通过 \code{ggrepel} 避免重叠。
}
\examples{
\dontrun{
rra_out <- run_rra_pipeline(sgrna_summary, library_df = lib)
plot_rra_volcano(rra_out[["linear"]], direction = "high")
}
}
